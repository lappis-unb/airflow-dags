dbt_pg_project:
  outputs:
    dev:
      host: 192.168.3.22
      port: 5432
      dbname: analytics
      user: "{{ env_var('DBT_POSTGRES_USER') }}"
      pass: "{{ env_var('DBT_POSTGRES_PASSWORD') }}"
      schema: dbt
      threads: 4
      type: postgres
  target: dev