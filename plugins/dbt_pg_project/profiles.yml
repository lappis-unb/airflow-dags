dbt_pg_project:
  outputs:
    dev:
      host: "{{ env_var('DBT_POSTGRES_HOST') }}"
      port: 50587
      dbname: analytics
      user: "{{ env_var('DBT_POSTGRES_USER') }}"
      pass: "{{ env_var('DBT_POSTGRES_PASSWORD') }}"
      schema: dbt
      threads: 4
      type: postgres
  target: dev